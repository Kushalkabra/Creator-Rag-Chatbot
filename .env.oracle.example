# Copy to .env in repo root before: docker compose up -d --build
# Replace YOUR_PUBLIC_IP with the VM's public IPv4 from Oracle console

PUBLIC_IP=YOUR_PUBLIC_IP
NEXT_PUBLIC_API_URL=http://YOUR_PUBLIC_IP:8000
ALLOWED_ORIGINS=http://YOUR_PUBLIC_IP:3000,http://localhost:3000
