# Railway — builds from repo root using ./Dockerfile (copies backend/)
# Alternative: set service Root Directory to "backend" and use backend/Dockerfile

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
