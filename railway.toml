# Railway — set service root directory to "backend" in the dashboard
# or deploy with: railway up (from backend/)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
